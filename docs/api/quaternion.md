# attitude.quaternion

Unit-quaternion algebra. Hamilton convention, scalar-first `[w, x, y, z]`, vectorized over `(..., 4)` arrays.

::: qnav.attitude.quaternion
    options:
      show_source: true
      members:
        - identity
        - mul
        - conjugate
        - inverse
        - normalize
        - canonical
        - rotate_vector
        - rotate_frame
        - exp
        - log
        - power
        - angle
        - axis
        - relative
        - angular_distance
        - mean
        - left_matrix
        - right_matrix
        - is_unit
        - random
        - from_scalar_last
        - to_scalar_last
        - from_jpl
        - to_jpl
